(function() {
            setTimeout(init, 100);

            function init() {

            	// //************************ reveal on load - index ********************************
                // var heroKv = new RevealFx(document.querySelector('.hero-kv'), {
                //     revealSettings: {
                //         bgcolor: '#8A2442',
                //         duration: 800,
                //         direction: 'rl',
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // heroKv.reveal();

                // //************************ reveal on load - about ********************************
                // var bigTitle = new RevealFx(document.querySelector('.f-container .ani-text h3'), {
                //     revealSettings: {
                //         bgcolor: '#7f40f1',
                //         duration: 1000,
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // bigTitle.reveal();

                // var subTitle = new RevealFx(document.querySelector('.f-container .head-super'), {
                //     revealSettings: {
                //         bgcolor: '#fcf652',
                //         delay: 850,
                //         duration: 800,
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // subTitle.reveal();

                //************************ reveal on load - contact ********************************
                // var whatsapp = new RevealFx(document.querySelector('#whatsapp'), {
                //     revealSettings: {
                //         bgcolor: '#7f40f1',
                //         delay: 500,
                //         duration: 1000,
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // whatsapp.reveal();

                // var whatsappSub = new RevealFx(document.querySelector('#whatsapp-sub'), {
                //     revealSettings: {
                //         bgcolor: '#fff',
                //         delay: 800,
                //         duration: 800,
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // whatsappSub.reveal();

                // var emailUs = new RevealFx(document.querySelector('#emailUs'), {
                //     revealSettings: {
                //         bgcolor: '#fcf652',
                //         delay: 1200,
                //         duration: 1000,
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // emailUs.reveal();


                // var emailUsSub = new RevealFx(document.querySelector('#emailUs-sub'), {
                //     revealSettings: {
                //         bgcolor: '#ccc',
                //         delay: 1500,
                //         duration: 800,
                //         onStart: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 0;
                //         },
                //         onCover: function(contentEl, revealerEl) {
                //             contentEl.style.opacity = 1;
                //         }
                //     }
                // });
                // emailUsSub.reveal();
            }


            //************************ scroll watcher ********************************

            // var myElement = document.querySelector(".flex-collection-cell.bottle");
            // var elementWatcher = scrollMonitor.create(myElement);

            // elementWatcher.enterViewport(function() {
            //     $('.flex-collection-cell.bottle svg').removeClass('stop-ani');
            // });
            // elementWatcher.exitViewport(function() {

            // });

        })();